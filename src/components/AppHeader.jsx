export default function appHeader() {

    // logic
    const title = "I miei post"

    // markup (JSX)
    return (
        <header>
            <div className="container">
                <h1>{title}</h1>
            </div>
        </header>
    )
}